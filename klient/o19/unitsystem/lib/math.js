/*add(numbers) {
    if(numbers.isArray() && numbers[0].isNumber){
        let sum = 0
    for(number of numbers){
        sum += number
    }
    return sum
    } else {
        return undefined
    }
}

prodcut(numbers) {

}*/